<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @contact.name %>
</p>
<!--
<ol>
  <% for question in @contact.addresses %>
  <li><%= question.email %></li>
  <% end %>
</ol>
-->

<% @contact.address.each do |add| %>
<p>
  <b><%= add[0] %>:</b>
  <%= add[1] %>
</p>
<% end %>


<%= link_to 'Edit', edit_contact_path(@contact) %> |
<%= link_to 'Back', contacts_path %>
