<%= form_for(@challenge) do |f| %>
  <% if @challenge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@challenge.errors.count, "error") %> prohibited this challenge from being saved:</h2>

      <ul>
      <% @challenge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <% for task in @challenge.tasks %>
  <%= fields_for "challenge[tasks][]", task do |task_form|%>
  <div class="field">
  	<p>
    	Score<br />
    	<%= task_form.text_field :score%>
		<%= task_form.text_field :score %>
	</p>
	<p>
		Score By<br />
    	<%= task_form.text_field :score_by %>
		<%= task_form.text_field :score_by %>
    </p>
  </div>
  <% end %>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
